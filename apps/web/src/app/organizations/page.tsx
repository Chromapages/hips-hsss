import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Organization Partnership — H.I.P.S.',
  description: 'Inquire about organizational partnerships and custom peer support programs.',
};

export { default } from './OrganizationsPageClient';
