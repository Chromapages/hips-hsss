VOICE_CHANGER_PORT=18888
VOICE_CHANGER_EXTERNAL_IP=127.0.0.1
VOICE_CHANGER_ALLOWED_ORIGIN=https://your-hardened-domain.example

# CPU smoke test:
#   docker compose --env-file .env up -d
#
# NVIDIA GPU test:
#   docker compose --env-file .env -f docker-compose.yml -f docker-compose.gpu.yml up -d
