// Stub for the `server-only` Next.js convention package.
// In production, this import throws if the bundler sees it reach a client
// component. Under vitest it's a harmless empty module.
export {};
